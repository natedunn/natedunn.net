---
import Link from './Link.astro';

interface Props {
  title: string;
  url: string;
  description: string;
}

const { title, url, description } = Astro.props;
---

<article class='dark:bg-slate-800 bg-slate-200 rounded-lg p-10 flex flex-col h-full'>
  <h3 class='text-2xl font-bold'>
    {title}
  </h3>
  <div class='mt-2 mb-8 italic'>{description}</div>
  <div class='mt-auto'>
    <Link href={url}>See example ⭢</Link>
  </div>
</article>
