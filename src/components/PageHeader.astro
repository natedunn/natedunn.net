---
type Props = {
  title: string;
  subtitle?: string;
};

const { title, subtitle } = Astro.props;
---

<div class='space-y-2 border-b border-zinc-200 dark:border-zinc-700 pb-6 mb-8'>
  <h1 class='text-4xl font-bold'>{title}</h1>
  {subtitle && <h2 class='text-lg'>{subtitle}</h2>}
</div>
